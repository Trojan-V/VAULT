package me.vault.vaultgame.city.controller;

import me.vault.vaultgame.city.model.Building;

/**
 *
 */
public class BuildingController
{
	/**
	 * Elevates the {@link Building} to the next (higher) level, updates the sprite and unlocks new features based on
	 * the new level.
	 *
	 * @param building The building which is meant to be upgraded.
	 * @since 06.05.2024
	 */
	public static void upgrade (Building building)
	{
//		int currentLevel = building.getCurrentLevel();
//		building.setCurrentLevel(++ currentLevel);
	}
}
